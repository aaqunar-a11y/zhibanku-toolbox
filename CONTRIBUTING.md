# 贡献指南

感谢愿意花时间改进这个仓库。为了避免返工，先说明它的两条硬性取向。

## 两条硬约束

**1. 零依赖。**

`src/` 与 `cli/` 下不得引入任何第三方运行时依赖。这不是洁癖，而是这个仓库存在的理由：用户会把它拷到一个断网的机器上用。因此：

- 哈希算法必须自己按公开标准实现，不从 npm 拉包，也不复制别人的实现代码
- 新增功能优先用语言内置能力解决（Node：`fs` / `crypto` / `path`；浏览器：WebCrypto、Blob、FileReader）
- 测试脚本同理，不引入 jest / mocha 之类框架

第三方包只能出现在开发工具链里（例如浏览器端到端测试用的 `playwright-core`），并且要用 `--no-save` 或 `devDependencies` 装，保证 `npm install` 之后运行时依赖数依然是 0。

**2. 每个结论都要能被独立复核。**

不允许出现"我们测过了，是对的"这种无法复核的说法。

- 新增算法 → 必须附**已知答案向量（KAT）**，判据来自公开标准（FIPS / RFC）或官方测试集，并注明出处
- 新增代码路径 → 必须与一个**独立实现**交叉校验（Node 用内置 `crypto`，浏览器用 WebCrypto）
- 涉及填充、分块、长度字段的改动 → 必须覆盖 55/56/57/63/64/65/119/120/121 这些边界长度**逐字节喂入**
- 性能或内存相关的说法 → 必须给出实测数字和测量方法，不能是估算

## 开发流程

```bash
git clone https://github.com/zhibanku/toolbox.git
cd toolbox

npm test              # 全量：115 个用例
npm run test:quick    # 跳过 64 MiB 与百万字符等耗时用例

npm run build         # 改了 src/hashkit.js 后必须重新生成 web/
npm run test:browser  # 真实 Chrome 端到端（需先 npm install --no-save playwright-core）
```

**改了 `src/hashkit.js` 就一定要重跑 `npm run build`。** 网页版内嵌的库源码由构建脚本注入，`test/run.js` 里有一条 `WEB-hashkit-sync` 专门断言"页面内嵌的实现与库源码一致"，两处漂移会让测试直接失败。

## 提交前自查

一份可以合并的改动，通常满足：

- [ ] `npm test` 全绿（输出里 `FAIL 0`）
- [ ] 若改了 `src/hashkit.js`，已重新 `npm run build` 并重跑测试
- [ ] 若改了页面模板，`npm run test:browser` 全绿，且移动端（375px）与键盘可达用例未被削弱
- [ ] 新增的每个判断都有可复核的判据，且判据来源写在测试里的注释或说明中
- [ ] 没有为"通过测试"而放宽断言阈值；如果确实需要调整阈值，在提交信息里说明依据
- [ ] `CHANGELOG.md` 已更新

## 明确的红线

下面这些改动会被直接拒绝：

- **放宽或删除断言来让测试通过。** 特别是网页静态门禁里"不上传 / 无外部依赖"那几条——它们是这个工具的核心主张，不是形式。
- **为通过测试而伪造行为。** 例如让 `hashBlobNative` 在 WebCrypto 不可用时返回一个看起来正确的值，或在页面上把 MD5 标成"已互证"。
- **复制第三方实现代码**后当作自己的实现提交。按公开标准独立实现是可以的，抄过来贴上 LICENSE 不行。
- **在页面产物里塞 SEO 内容**（关键词堆砌、与工具无关的长文案、外链农场）。这是工具页，不是软文页。
- **编造元数据**：虚假的 star 数、下载量、CI 徽章、测试覆盖率，或声称在未实测过的平台上可用。
- **引入会向外发起请求的代码**（统计、CDN、遥测、错误上报）。网页产物的网络请求数必须是 0。

## 代码风格

- `src/` 与 `cli/` 保持 **ES5**（`var` + 函数声明）：`src/hashkit.js` 要能被老浏览器直接 `<script>` 引入，不做转译
- `test/`、`tools/`、`browser-test/` 可用现代语法（Node ≥ 18）
- 缩进 2 空格，不用分号以外的花活；注释写"为什么"，不写"是什么"
- 注释用中文；标识符、错误码、目录名用英文

## 报告问题

提 issue 时请附上：操作系统与版本、Node 版本（或浏览器与版本）、复现命令、**期望的哈希值与实际得到的哈希值**。如果涉及某个具体文件，请说明文件大小，以及能否用 `sha256sum` / `certutil` 得到不同的结果——这能立刻区分"是库的问题"还是"文件本身被改动过"。
